This should be running on port 4567(the default port for spark web)
if changes are needed, the start() method in Core.Server has the option to change the port number.

To run the project, mvn compile and run Main.
More info will be shown on "localhost:4567"
(index page located under resources/spark/template/freemarker)